---
description: 
globs: 
alwaysApply: true
---
---
description: Always use PapaParse for CSV import/export routines
globs: [src/**/*.js, src/**/*.jsx]
alwaysApply: true
---
- All code that parses, serializes, or validates CSV data must leverage PapaParse.
- Never implement ad-hoc CSV parsing logic.
- Example import: `import Papa from "papaparse"`